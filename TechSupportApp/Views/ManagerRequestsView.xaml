<UserControl x:Class="TechSupportApp.Views.ManagerRequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="5">
            <Label Content="Поиск:"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="5"/>
            <Label Content="Исполнитель:"/>
            <ComboBox ItemsSource="{Binding Executors}" DisplayMemberPath="FullName" SelectedValuePath="Id"
                      SelectedValue="{Binding SelectedExecutorId}" Width="150" Margin="5"/>
            <Button Content="Искать" Command="{Binding SearchCommand}" Margin="5"/>
            <Button Content="Сбросить" Command="{Binding ResetCommand}" Margin="5"/>
            <Button Content="Добавить заявку" Command="{Binding AddRequestCommand}" Margin="5"/>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Requests}" AutoGenerateColumns="False"
                  SelectedItem="{Binding SelectedRequest}" Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding CreateDate, StringFormat=dd.MM.yyyy}"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding ClientPhone}"/>
                <DataGridTextColumn Header="Тип техники" Binding="{Binding EquipmentType.Name}"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status.Name}"/>
                <DataGridTextColumn Header="Приоритет" Binding="{Binding Priority.Name}"/>
                <DataGridTextColumn Header="Исполнитель" Binding="{Binding AssignedToUser.FullName}"/>
                <DataGridTextColumn Header="Дата завершения" Binding="{Binding CompletionDate, StringFormat=dd.MM.yyyy}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Редактировать" Command="{Binding EditRequestCommand}" Margin="5"/>
            <Button Content="Удалить" Command="{Binding DeleteRequestCommand}" Margin="5"/>
            <Button Content="Подробнее" Command="{Binding ShowDetailsCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>